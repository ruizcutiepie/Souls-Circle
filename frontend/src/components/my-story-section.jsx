// frontend/src/components/my-story-section.jsx

import React from "react";

// Legacy component - now disabled because the new story section
// lives directly inside LandingPage.jsx
const MyStorySection = () => {
  // Render nothing
  return null;
};

export default MyStorySection;
